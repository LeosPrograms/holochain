- need a way to detect when quorum is lost
- parallel network, multiple leaders
- can there be a leader even if quorum is lost?
- can an arbitrary snapshot be loaded in at any time? log compaction?

features:
- see how it works with partitions
- ability to install an arbitrary snapshot
- 